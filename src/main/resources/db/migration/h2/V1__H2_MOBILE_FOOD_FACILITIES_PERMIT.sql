CREATE TABLE MOBILE_FOOD_FACILITIES_PERMIT (
	LOCATION_ID INTEGER NOT NULL,
	APPLICANT VARCHAR(128),
	FACILITY_TYPE VARCHAR(50),
	CNN INTEGER,
	LOCATION_DESCRIPTION VARCHAR(128),
	ADDRESS VARCHAR(50),
	BLOCKLOT VARCHAR(50),
	BLOCK VARCHAR(50),
	LOT VARCHAR(50),
	PERMIT VARCHAR(50),
	STATUS VARCHAR(50),
	FOOD_ITEMS VARCHAR(512),
	X REAL,
	Y REAL,
	LATITUDE REAL,
	LONGITUDE REAL,
	SCHEDULE VARCHAR(256),
	DAYSHOURS VARCHAR(50),
	NOISENT VARCHAR(50),
	APPROVED VARCHAR(50),
	RECEIVED INTEGER,
	PRIOR_PERMIT INTEGER,
	EXPIRATION_DATE VARCHAR(50),
	LOCATION VARCHAR(50),
	FIRE_PREVENTION_DISTRICTS INTEGER,
	POLICE_DISTRICTS INTEGER,
	SUPERVISOR_DISTRICTS INTEGER,
	ZIP INTEGER,
	NEIGHBORHOODS_OLD INTEGER,
	CONSTRAINT MOBILE_FOOD_FACILITIES_PERMIT_PK PRIMARY KEY (LOCATION_ID)
);

INSERT INTO MOBILE_FOOD_FACILITIES_PERMIT
SELECT * FROM CSVREAD('classpath:Mobile_Food_Facility_Permit.csv', NULL, 'charset=UTF-8 fieldSeparator=,');
